apiVersion: v1
kind: Pod
metadata:
  name: healthy
spec:
  restartPolicy: Always
  containers:
  - name: healthy
    image: busybox
    command: ["/bin/sh", "-c"]
    args: ["while true; do echo ok; sleep 5; done"]
